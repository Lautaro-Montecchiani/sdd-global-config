# Claude Code — configuración local de sesión (machine-specific)
.claude/settings.local.json

# Secrets
.env
*.env
*.key
*.pem
*.p12
secrets/

# Configuración personal del CLI del Agente (permisos, workspaces, rutas de la PC): se instala desde templates/
.gemini/
