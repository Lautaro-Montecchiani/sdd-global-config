## 1. Preparación

- [x] 1.1 Definir `{proyectos}` como la carpeta que contiene `sdd-global-config` y confirmar cuáles de `{proyectos}\seguimiento-Suscripciones`, `{proyectos}\interfaz-formulario-licitación` y `{proyectos}\normalizacionLeads-contactacion` existen. No clonar ninguno: si falta alguno, dejar sus tareas sin marcar y avisar al usuario (ver D6)
- [x] 1.2 Escribir un script en el scratchpad de la sesión, fuera de todo repo, `check-claude-md.ps1`, que reciba la ruta de un `CLAUDE.md` y verifique: (a) existe la sección "Workflow SDD / OpenSpec" con `~/.claude/CLAUDE.md` y `/opsx:propose`; (b) no aparecen "prompt para Copilot", "Bash Headless", "OPCX" ni "Fase 0 — LEER"; (c) no aparece `C:\Users`. Además debe listar, para revisión manual, las líneas con "Copilot", "Gemini", "Task.md", "spec.yaml" y "change-map"

## 2. interfaz-formulario-licitación

- [x] 2.1 En el repo `{proyectos}\interfaz-formulario-licitación`: guardar la salida de `git status --short`, ejecutar `git fetch origin master` y crear el worktree con `git worktree add "$env:TEMP\align-claude-md\interfaz" -b change/align-project-claude-md-openspec origin/master`. No modificar el árbol de trabajo del repo principal. Comparar con `git diff origin/master -- CLAUDE.md` si la rama activa cambió `CLAUDE.md` y avisar al usuario si difiere
- [x] 2.2 Leer `{proyectos}\interfaz-formulario-licitación\.github\workflows\spec-check.yml`, `{proyectos}\interfaz-formulario-licitación\docs\TEMPLATES.md` y `{proyectos}\interfaz-formulario-licitación\ADRs\` para confirmar qué referencias del `CLAUDE.md` siguen vigentes
- [x] 2.3 Reescribir `"$env:TEMP\align-claude-md\interfaz\CLAUDE.md"` según las decisiones D1–D3 y la spec `project-claude-md`: quitar "Rol de Claude en este proyecto" y "Reglas globales (ver docs/TEMPLATES.md)"; agregar la sección "Workflow SDD / OpenSpec"; retitular "Estructura de features (specs/)" como specs legacy de referencia; conservar "Stack", "Restricciones del entorno GAS", "IDs de recursos externos" y "Riesgos pendientes"; conservar de las reglas viejas solo lo que la tarea 2.2 confirme vigente (por ejemplo `spec-check.yml` sobre los `specs/` legacy o los ADRs)
- [x] 2.4 Ejecutar `check-claude-md.ps1` sobre ese archivo sin fallas, revisar las coincidencias manuales y confirmar con `git diff origin/master -- CLAUDE.md` en el worktree que las únicas secciones cambiadas son las previstas
- [x] 2.5 Revisar `git diff --staged` en el worktree y commitear solo `CLAUDE.md` con `docs: alinear CLAUDE.md con el flujo OpenSpec global` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear

## 3. seguimiento-Suscripciones

- [x] 3.1 En el repo `{proyectos}\seguimiento-Suscripciones`: guardar la salida de `git status --short`, ejecutar `git fetch origin master` y crear el worktree con `git worktree add "$env:TEMP\align-claude-md\suscripciones" -b change/align-project-claude-md-openspec origin/master`. Avisar al usuario si `CLAUDE.md` difiere entre la rama activa y `origin/master`
- [x] 3.2 Leer `{proyectos}\seguimiento-Suscripciones\specs\manifest.yaml` para confirmar las features legacy que se listarán como referencia
- [x] 3.3 Reescribir `"$env:TEMP\align-claude-md\suscripciones\CLAUDE.md"` según D1–D3: conservar "Proyecto", "Arquitectura", "Specs de comportamiento", "Reglas de arquitectura", "Watchlist" y "Estado del proyecto"; agregar "Workflow SDD / OpenSpec" y una sección corta de specs legacy (features del manifest, como referencia); eliminar "Reglas de comunicación" y desde "Sistema SDD (Spec-Driven Development)" hasta "Ventajas, Riesgos y Roadmap" inclusive, incluida "Seguridad" y "Estrategia de Modelos"; reemplazar las rutas con `C:\Users\...` (línea "Repo" y rutas de entrada del scraper) por `{proyectos}\...`; reescribir solo la fila "SDD / specs machine-readable" de la tabla de estado y dejar las demás filas igual
- [x] 3.4 Ejecutar `check-claude-md.ps1` sobre ese archivo sin fallas, revisar las coincidencias manuales y confirmar con `git diff origin/master -- CLAUDE.md` que "Specs de comportamiento", "Reglas de arquitectura" y "Watchlist" no cambiaron salvo las rutas
- [x] 3.5 Revisar `git diff --staged` en el worktree y commitear solo `CLAUDE.md` con `docs: alinear CLAUDE.md con el flujo OpenSpec global` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear

## 4. normalizacionLeads-contactacion

- [x] 4.1 En `D:\Proyectos\normalizacionLeads-contactacion` (rama activa `master`, con el commit local de limpieza `2ba2ca4`, `.claude/settings.local.json` modificado y `scratch/` sin versionar): guardar `git status --short`, ejecutar `git fetch origin master` y crear el worktree con `git worktree add "$env:TEMP\align-claude-md\leads" -b change/align-project-claude-md-openspec origin/master`. Su `CLAUDE.md` local coincide con `origin/master` (verificado al verificar el change). No tocar el árbol de trabajo del repo principal
- [x] 4.2 Reescribir `"$env:TEMP\align-claude-md\leads\CLAUDE.md"` según D1–D3 y D7: conservar "Proyecto", "Arquitectura del Sistema", "Specs de Comportamiento", "Reglas de Arquitectura" y "Watchlist"; agregar "Workflow SDD / OpenSpec" (que diga también que los changes nuevos se crean en `openspec/changes/`) y una línea que aclare que `{proyectos}` es la carpeta que contiene los repos del usuario; eliminar "Reglas de Comunicación" y "Sistema SDD (Spec-Driven Development) — Modo Híbrido" completo, salvo la tabla "Features SDD Activas", que se conserva como "Features legacy de referencia" sin la nota "Para agregar una feature: crear `specs/{slug}/…`"; reemplazar la ruta de la línea "Repo" por `{proyectos}\normalizacionLeads-contactacion` y la ruta al escritorio de la regla "Rutas absolutas en Power Query" por `{escritorio}\normalizacionLeads` con la aclaración de que es el escritorio del usuario de cada equipo, sin cambiar el resto de la regla
- [x] 4.3 Ejecutar `check-claude-md.ps1` sobre ese archivo sin fallas, revisar las coincidencias manuales, confirmar con `git diff origin/master -- CLAUDE.md` que "Proyecto", "Arquitectura del Sistema", "Specs de Comportamiento", "Reglas de Arquitectura" y "Watchlist" no cambiaron salvo las rutas, revisar `git diff --staged` y commitear solo `CLAUDE.md` con `docs: alinear CLAUDE.md con el flujo OpenSpec global` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear

## 5. Correcciones del verify

- [x] 5.1 En `{proyectos}\interfaz-formulario-licitación`: recrear el worktree sobre la rama existente con `git worktree add "$env:TEMP\align-claude-md\interfaz" change/align-project-claude-md-openspec` y editar `"$env:TEMP\align-claude-md\interfaz\CLAUDE.md"`: en "Specs legacy de referencia" decir que las funcionalidades pre-OpenSpec están documentadas en `specs/` y que los changes nuevos se crean en `openspec/changes/`, y precisar que `spec-check.yml` corre en los PRs que tocan `specs/**`, `*.gs` o `*.html` y ejecuta `spec_lint.py` y `verify_spec_parity.py`. Ejecutar `check-claude-md.ps1` sin fallas y commitear solo `CLAUDE.md` como commit nuevo (sin amend) con `docs: precisar specs legacy y CI en CLAUDE.md` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear
- [x] 5.2 En `{proyectos}\seguimiento-Suscripciones`: recrear el worktree sobre la rama existente con `git worktree add "$env:TEMP\align-claude-md\suscripciones" change/align-project-claude-md-openspec` y editar `"$env:TEMP\align-claude-md\suscripciones\CLAUDE.md"`: en la fila "Spec y reglas de negocio" de la tabla de estado cambiar `spec.md` y `rules_suscripciones_toyota.md` por `docs/spec.md` y `docs/rules_suscripciones_toyota.md` (confirmar antes con `git ls-tree`), y agregar una línea que aclare que `{proyectos}` es la carpeta que contiene los repos del usuario. Ejecutar `check-claude-md.ps1` sin fallas y commitear solo `CLAUDE.md` como commit nuevo (sin amend) con `docs: corregir rutas y aclarar placeholder en CLAUDE.md` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear

## 6. Ajustes del segundo verify

- [ ] 6.1 En `D:\Proyectos\normalizacionLeads-contactacion`: recrear el worktree sobre la rama existente con `git worktree add "$env:TEMP\align-claude-md\leads" change/align-project-claude-md-openspec` y editar `"$env:TEMP\align-claude-md\leads\CLAUDE.md"`: (a) en la regla "Rutas absolutas en Power Query" dejar entre backticks solo `{escritorio}\normalizacionLeads` y mover la aclaración fuera del bloque de código ("donde `{escritorio}` es el escritorio del usuario de cada equipo"), sin cambiar el resto de la regla; (b) al inicio de "Features legacy de referencia" agregar una línea que diga que esas features están documentadas en `specs/` y que los changes nuevos se crean en `openspec/changes/`, y agregar el separador `---` antes de "## Watchlist (problemas conocidos)". Ejecutar `check-claude-md.ps1` sin fallas, revisar `git diff --staged` y commitear solo `CLAUDE.md` como commit nuevo (sin amend) con `docs: ordenar regla de rutas y sección legacy en CLAUDE.md` y el trailer `Spec-ID: align-project-claude-md-openspec`. No pushear ni eliminar el worktree: se usa en 7.2
- [ ] 6.2 Ejecutar `git worktree prune` en `{proyectos}\interfaz-formulario-licitación` y en `{proyectos}\seguimiento-Suscripciones`, y confirmar con `git worktree list --porcelain` en cada uno que no queda ninguna entrada `prunable` y que la rama `change/align-project-claude-md-openspec` sigue existiendo con sus dos commits

## 7. Verificación

- [ ] 7.1 En este repo, ejecutar `openspec validate align-project-claude-md-openspec` sin errores; confirmar con `git -C <repo> show change/align-project-claude-md-openspec:CLAUDE.md` que las ramas de los tres proyectos (`interfaz-formulario-licitación`, `seguimiento-Suscripciones` y `normalizacionLeads-contactacion`) tienen el `CLAUDE.md` commiteado, y ejecutar `check-claude-md.ps1` sobre el contenido de cada rama
- [ ] 7.2 Verificar que el cambio aplica en un proyecto de prueba: en el worktree de `normalizacionLeads-contactacion` ejecutar `claude -p` sin herramientas con la pregunta "¿Cuál es el flujo para implementar una feature nueva en este proyecto?" y confirmar que la respuesta menciona `/opsx:propose` y el handoff al Agente, y que no ofrece generar un prompt para Copilot ni crear `spec.yaml` o `Task.md`. Si `claude -p` no está disponible, pedir al usuario que lo pruebe en una sesión nueva. Al terminar, eliminar el worktree de leads con `git worktree remove`, ejecutar `git worktree prune` en los tres repos y confirmar con `git worktree list --porcelain` que ninguno queda con entradas `prunable`, eliminar la carpeta `"$env:TEMP\align-claude-md"` si quedó vacía, confirmar que `git status --short` de cada repo principal coincide con el guardado y que las ramas quedan locales, y avisar al usuario: reiniciar las sesiones abiertas de esos proyectos, decidir push y PR de cada repo con `/branch-pr`, que `interfaz-formulario-licitación` no tiene `.gitignore` en la raíz y que `openspec/config.yaml` de `normalizacionLeads-contactacion` es la plantilla por defecto, sin `project_type`
